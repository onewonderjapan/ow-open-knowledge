
| 缺口 | 本修订后的状态 | 政策出口 |
|---|---|---|
| **脱敏执行角色** | 步骤与三件套已写；谁执行、是否走 ai-stack **未决**。未决前停在待脱敏 | 拍板 #7 |
| **晋升门 Owner** | 硬条件与 PRM 格式已写；谁有权 merge **未决**。未决前不得 merge | 拍板 #4 |
| **机主批准点** | 运行时与附录 B 对齐；清单范围 **未决** | 拍板 #8 |
| **排程归属** | 自检 + 日向必扫 + ALT 升级已写；登记 Owner **未决**。未决前不声称 crontab 已注册 | 拍板 #9 |
| **调查/隔离/审计物理仓** | 逻辑路径已写，物理仓 **未决**。未决前不 push 到未定仓 | 拍板 #2 #3 |
| **本文件目录归属** | 仍意图放 `docs/`（新建），与主题目录制不完全一致 | 拍板 #1 |
| **FDE 轨状态** | 交接分支本次仍未找到，保持未核实 | 拍板 #6 |
| **咨询/算命 Bot** | 已排除默认排程；补了 24h 点名响应。若要进日历另拍板 | 维持「按需」 |
| **意图时刻时区** | 正文不写死 TZ | 拍板 #11 |
| **分析门发起 Bot** | 运行默认为秘书排队 + 源 Bot 补输入；正式归属未决 | 拍板 #12 |
| **告警外部通道** | 文件内 ALT 为第一落点；IM/邮件未核实 | 拍板 #13 |
| **内容仓子目录** | 不写死；用各仓既有约定 | 保持未核实，不另造政策 |

### 5.3 每周闭环抽查清单（秘书 · 周五 17:40）

- [ ] 本周 INV 是否都有下游（整理 / 关闭 / 分析 / 显式「无需下游」）？
- [ ] `{ISOLATION_ROOT}/external-exchange/` 是否零写入正式 KB / CDN / 公开主题目录？
- [ ] AN 输出是否都拆了「可入库 / 待验证」（允许另有「明确丢弃」）？
- [ ] 标「可入库」的条目是否都有 PRM 或显式「暂不申请」？未批准是否都未 merge？
- [ ] 对外发言是否仅来自 neng社交的，且 `-out.md` 抽查=通过（或该周无外发且有空窗）？
- [ ] 待脱敏是否零 live、零公开晋升？
- [ ] ALT 是否都在日向出现过，连续失败是否已升级？
- [ ] 管仓库的周日报告（上一周日）是否有孤儿项与认领人（认领 ≠ 删除）？

### 5.4 周日抽查清单（秘书 · 12:00，依赖 11:00 审计已交卷）

- [ ] `{OPS_ROOT}/audits/YYYY-MM-DD-repo-orphan.md` 是否存在且非空？
- [ ] 三套超期阈值是否都列了，且无「已自动删除」？
- [ ] 公开仓 / knowledge-hub / CDN 误写是否为零；若有，是否已停窗并 ALT + 确认门？
- [ ] 周五清单遗留项是否逐条有 Owner 与下一动作（不重做周五全文）？

---

## 6. 需要拍板

> 原 1–10 编号与 A/B/C **原文保留**。仅在部分项后加「本修订注」，不加新选项、不删问题。11 起为追加项。

1. **本计划文件长期归属**  
   - **A**：保留 `ow-open-knowledge/docs/`（新建运营计划区）  
   - **B**：迁入 `team-norms/`  
   - **C**：迁入 `workflow-standard/` 或改放到私有 `ai-ops`  
   - 说明：当前仓根无既有 `docs/`，主题目录制更常见。

2. **调查记录 / 项目快照 / 审计报告的物理仓与根路径**  
   - **A**：新建私有 `ow-bot-ops`（或等价）统一放 `investigations/` `projects/` `audits/`  
   - **B**：调查与日更进 `owd-daily-intel`；审计进 `ai-ops/audits/`  
   - **C**：仍放 W2 知识库正本目录，Git 仓只存晋升后副本  
   - 本修订注：若选 B，须另定调查**正本**与「今日份」指针的关系，避免全文进日更；`briefs/` `staging/` `promotions/` `schedules/` `alerts/` 是否同根，请一并指定。未决前只使用逻辑路径。

3. **社交隔离区位置**  
   - **A**：与 #2 同一私有 ops 仓的 `external-exchange/`  
   - **B**：独立私有仓（更严隔离）  
   - **C**：仅本地盘 + 定期打包，不进 Git  
   - 本修订注：`outbound-queue/` 是否与隔离区同根，随本项一并定。未决前禁止写入公开仓。

4. **「可入库」晋升门审批人**  
   - **A**：仅机主  
   - **B**：机主 + 学我说话的秘书代行（低风险 L1）  
   - **C**：按仓指定 Maintainer（open-knowledge / knowledge-hub 设计负责人 / ai-ops）  
   - 本修订注：未决前一律未批准、不得 merge。附录 B 确认门不可被「低风险 L1 代行」绕过。

5. **暂存区（调查/隔离/待验证）超期天数 N**  
   - **A**：14 天  
   - **B**：30 天  
   - **C**：按 L 级不同（L1=30 / L2=14 / L3=7）  
   - 本修订注：原文曾写「N 默认 14」，等于未拍板先锁死 A。已撤销该默认；周审计用三套阈值对照，不自动归档删除。

6. **FDE 交接轨如何记账**  
   - **A**：分支已废弃，从活跃轨剔除  
   - **B**：文档已合入 `main` 其他路径——指定新 path 后纳入官网轨  
   - **C**：分支改名/私有化——提供现名后再写入 §2  
   - 本修订注：本修订仍未取到 `handoff/fde-ab-demo-kickoff`，保持未核实，不选 A/B/C。

7. **脱敏执行归属**  
   - **A**：整理类 Bot 自脱敏，秘书抽查  
   - **B**：固定走 ai-stack 脱敏管线（或其后继），产出三件套再外发/入库  
   - **C**：仅人工脱敏，Bot 只打「待脱敏」标签  
   - 本修订注：§4.6 / 附录 D 已写步骤与三件套文件名。#7B 管线是否可调用**未核实**。未决前停在待脱敏，不外发、不晋升。

8. **必须机主批准的动作清单（确认门）**  
   - **A**：采用本计划附录默认集（改五规则、改 Bot 名、删文件、对外 live、晋升公开仓、动用客户 L3）  
   - **B**：在 A 基础上扩大到「一切合入 main」  
   - **C**：缩小到「钱 / 客户数据 / 对外发布」三项（对齐现有 routing 验收原则）  
   - 本修订注：未决前运行对齐附录 B（即 A 的清单），这是保守停窗，不是把 #8 选成 A。

9. **定时排程的 Owner**  
   - **A**：学我说话的秘书统一登记与值守  
   - **B**：各 Bot 自管，管项目的做汇总  
   - **C**：交 `ai-ops` 路由表扩展一节「Bot cron 意图表」由指定 AI 值守  
   - 本修订注：§4.8 已写自检 + 日向必扫 + ALT。未决前不注册具体调度器、不指定唯一 Owner。

10. **是否将本协作计划中的内部 Bot 名与排程视为 L2（社内）而不随 open-knowledge 一并公开**  
    - **A**：本文件暂留 open-knowledge，公开前另出脱敏版  
    - **B**：立即改放到私有仓，open-knowledge 只留无人名的方法论摘要  
    - **C**：维持公开，接受 Bot 昵称外露  

11. **意图时刻使用的时区**（追加）  
    - **A**：机主本地工作时区（拍板时写明 IANA 名，例如 Asia/Tokyo）  
    - **B**：统一 UTC  
    - **C**：按 Bot 所在运行机（`ai-ops/machines/`）分别标注  
    - 说明：2026-09-11 调研未核实运行 TZ。未决前按「机主惯常工作日」理解，不写进 crontab。

12. **每日分析门的发起 Bot**（追加）  
    - **A**：学我说话的秘书统一排队并出 AN  
    - **B**：源 Bot 各自出 AN，秘书只做门前队列与抽查  
    - **C**：研究爆款的兼每日分析窗，秘书只在周五收周报  
    - 说明：五条规则未指定分析执行者。§4.4 运行默认接近 B + 秘书列队，不是已选。

13. **失败告警除文件外是否另接外部通道**（追加）  
    - **A**：仅 `{OPS_ROOT}/alerts/` + 秘书日向，不接 IM  
    - **B**：文件为 SSOT，另接现网 IM（拍板时写通道名；**现网通道本次未核实**）  
    - **C**：文件 + 仅确认门级失败才人工呼叫机主  
    - 说明：未决前只保证 ALT 文件 + 日向必扫，不声称已通知到人。

---


11. **（若尚未列出）时区** — 见既有 #11；本修订不改其选项。

14. **Grok 专用分支命名**  
   - **A**：统一 `grok/knowledge`  
   - **B**：统一 `grok/inbox`  
   - **C**：按仓自定义，但必须在登记表写明  

15. **Grok 输送文件在仓内的相对路径**  
   - **A**：统一 `grok-inbox/YYYY-MM-DD-<slug>.md`  
   - **B**：统一 `docs/grok/YYYY-MM-DD-<slug>.md`  
   - **C**：跟随各仓既有 docs 习惯，由专管 Bot 在登记表声明  

16. **「进行中」仓库最终名单与专管 Bot 策略**  
   - **A**：采用 §8.3 候选全集，现有 Bot 兼任 + 缺口再新建  
   - **B**：只覆盖 §2.1 近 1–2 周活跃轨，其余暂缓  
   - **C**：你给出白名单；其余仓一律不建专管  

17. **是否立即批量创建缺失的专管 Bot / 分支**  
   - **A**：先只改计划与登记表，不建 Bot、不开分支  
   - **B**：先开各仓 Grok 分支（空 commit / README），Bot 后建  
   - **C**：分支 + 专管 Bot 一起建（需你确认命名规则后执行）  


## 7. 不做什么

1. **不** merge 本分支；不删除既有文件；不改各产品/应用代码。本修订只覆盖写入这一份 markdown。  
2. **不**静默改写机主五条共通规则；政策变更只进「需要拍板」。  
3. **不**在未拍板前重命名 Bot。  
4. **不**让非社交 Bot 直接对外部社区发言。  
5. **不**把外部习得或待验证条目写入既有 KB / CDN 知识数据 / 公开主题目录。  
6. **不**把 `owd-knowledge-hub` 当内容笔记本用。  
7. **不**在未确认门情况下删除仓库文件或路径。  
8. **不**把咨询/算命 Bot 拉进默认工作日 cron。  
9. **不**编造未核实的仓库状态（如本次未找到的 FDE 分支内容、skill-platform 空描述、内容仓子目录、ai-stack 脱敏管线可用性、现网 IM 通道）。  
10. **不**用本计划替代 `ai-ops` 的规则/路由/仓库总账正本。  
11. **不**在未拍板 #16/#17 前批量创建专管 Bot 或向 `main` 推 Grok 知识。  
12. **不**把 Grok 专用分支上的建议视为已合并事实。  
  
11. **不**把逻辑路径 `{OPS_ROOT}` `{ISOLATION_ROOT}` 当成已存在、已选定的 Git 目录去 push。  
12. **不**把拍板未决项写成已决默认（包括但不限于 N=14、审计挂死 `ai-ops`、分析窗已指定专职 Bot、crontab 已注册）。  
13. **不**在失败后自动补发对外内容；不把「16:00 无队列」写成失败。  
14. **不**让 `ai-knowledge-local` 投影早于正本，造成双权威。

---

## 附录 A · 调研方法摘要

- 组织仓检索：`org:onewonderjapan`，按 `pushed_at` 核对已知清单。  
- 深读：`ow-open-knowledge` README.zh-CN / CONTRIBUTING / `agent-cultivation/data-classification.md` / `workflow-standard/STANDARD.md`；`owd-knowledge-hub` README；`ai-ops` README + `AI_RULES.md` + `routing.md` + `repos.md`；`owd-daily-intel` README + `2026-09-11.md` + `INDEX.md`；`ai-knowledge-local` README。  
- `onewonder-homepage`：`handoff/fde-ab-demo-kickoff` 在调研时不可解析。  
- 本修订未重跑上述检索，不更新 §2 的 push 日期与「未核实」结论。

## 附录 B · 默认确认门（供拍板 #8 选项 A）

以下动作默认停下并请示机主：

1. 修改五条共通规则或本计划的政策段落；  
2. 重命名 / 裁撤 Bot；  
3. 删除文件或目录；  
4. 对外 live 发布（非模拟队列）；  
5. 晋升到公开 KB 或 CDN 知识数据；  
6. 处理客户 L3 原文（含解除伏字）。

拍板 #8 未决期间，运行按此清单**停窗请示**（保守执行，不等于已选定 A）。

## 附录 C · 文件名与 ID

| 类型 | ID 前缀 | 文件名模式 |
|---|---|---|
| 调查 | `INV-YYYYMMDD-NN` | `investigations/YYYY-MM-DD-<slug>.md` |
| 分析 | `AN-YYYYMMDD-NN` | `staging/AN-YYYYMMDD-NN.md`（或在头字段声明唯一正本 path） |
| 脱敏 | `RED-YYYYMMDD-NN` | `staging/redacted/RED-YYYYMMDD-NN-{src,out,map}.md` |
| 晋升 | `PRM-YYYYMMDD-NN` | `promotions/PRM-YYYYMMDD-NN.md` |
| 告警 | `ALT-YYYYMMDD-NN` | `alerts/ALT-YYYYMMDD-NN.md` |
| 日向 | 无（按日） | `briefs/YYYY-MM-DD-daily.md` |
| 周闭环 | 无（按日） | `briefs/YYYY-MM-DD-weekly-close.md` |
| 审计 | 无（按日） | `audits/YYYY-MM-DD-repo-orphan.md` |
| 项目快照 | 无（按日） | `projects/YYYY-MM-DD-board.md` |
| 空窗 | 无 | 可写在当日 briefs 或任务同目录 `YYYY-MM-DD-<task>-skip.md` |

相对目录均相对于 `{OPS_ROOT}` 或 `{ISOLATION_ROOT}`（见 §2.7）。`NN` 为当日两位序号。
