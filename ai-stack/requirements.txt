# The demo Web UI (ui/app.py) runs on the standard library only.
# janome improves the RAG layer (morphological analysis). The pipeline
# falls back to CJK bigrams if janome is not installed.
janome>=0.5.0,<1.0
