---
name: pre-task
description: 有风险任务开工前的逐项确认（数据分级/权限/预算/回滚）。缺失项=阻塞，不许假设。
---
# pre-task

按 S3 执行前确认:
1. 逐项确认: 数据等级(L1/L2/L3)、是否含客户实名/个资/密钥、代码仓库与分支、GPU/模型/预算上限、截止时间、审批人、回滚方案。
2. 输出确认清单: OK｜缺失｜风险｜需审批。
3. 触及L3数据、生产系统变更、预算消耗 → 标记「必须先审批」，停止并提示联系Team Lead。
4. 缺失项一律列为阻塞，禁止假设默认值。
