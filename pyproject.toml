[tool.ruff]
line-length = 120
target-version = "py39"
extend-exclude = [
  "ai-stack/out",
  "dev-pipeline/workspace",
  "dev-pipeline/output",
]

[tool.ruff.lint]
select = ["E", "F", "B"]
ignore = ["E501"]
